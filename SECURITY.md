# 安全政策

## 支持范围

项目尚在 `v0.1.0` 发布准备阶段。当前仅维护默认分支上的最新代码；早期提交和测试包不承诺安全更新。正式发布后会在此处列出受支持版本和修复策略。

本项目默认只提供本机回环访问，不应被视为可直接部署到公网的服务。将服务暴露到局域网或公网、使用反向代理、共享数据库或替换默认访问控制，均超出当前支持范围，需自行完成额外的安全评估。

## 报告漏洞

请勿在公开 Issue、讨论区、日志或截图中披露可复现的漏洞细节、密钥、用户材料或训练数据。

仓库启用 GitHub 私密漏洞报告后，请从仓库的 **Security → Advisories → Report a vulnerability** 提交。该入口尚未启用时，请通过你获得项目副本的私下渠道联系维护者，并提供：

- 受影响的版本或提交；
- 漏洞影响与复现所需的最小步骤；
- 可能的修复方向（如有）。

在确认修复计划前，请勿公开漏洞细节。维护者不会要求报告者在 Issue 中粘贴密钥、完整数据库或未经脱敏的个人训练数据。

## 安全边界与配置

- API Key 仅应放在 `backend/.env` 或本地设置中，绝不能写入前端、测试样本、Issue 或 Pull Request。
- 数据库、上传材料、报告、音频和日志可能包含个人信息，默认应保留在本地。
- 使用第三方 ASR、TTS 或 LLM Provider 时，相关音频、文本或提示内容会发送给你配置的服务商；请自行确认其数据处理条款。
- 下载或解析不可信材料前，请保持依赖为已修复的版本，并在隔离环境中进行测试。
