# 表达能力训练器 PRD

> 版本：v0.5
> 状态：一期开发基线
> 修订日期：2026-08-23

## 1. 产品定义

### 1.1 定位

本地优先的中文表达训练工具，通过个人声音校准、实时弱提示和场景化可解释报告，帮助用户看见自己“怎么说”，而不只评价“说了什么”。

项目不是通用 AI 面试聊天应用，也不提供心理、医疗或情绪诊断。

### 1.2 一期目标

发布 `v0.1.0`，证明同一套中文表达分析核心能够稳定服务三个现有场景：

- 模拟面试
- 工作汇报
- 演讲训练

三个场景共享收音、转写、个人基线、实时反馈、事实分析和数据存储；差异只进入阶段流程、语义评价标准与报告建议。

### 1.3 目标用户

- 希望反复练习中文表达、但不愿上传长期个人数据的本地用户
- 求职者：训练自我介绍、项目回答和岗位匹配表达
- 职场沟通者：训练工作汇报、述职和质询应对
- 演讲练习者：训练限时结构、节奏和核心观点
- 希望研究或改进可解释中文表达分析的开源开发者

### 1.4 核心价值

1. **可重复**：同一套事实指标可用于多次训练对比。
2. **个体化**：声音变化优先相对个人基线解释，不套用单一人群阈值。
3. **可解释**：每项评价显示来源、权重、证据和数据是否充分。
4. **低干扰**：实时反馈只做弱提示，完整评价留到训练结束后。
5. **本地优先**：数据默认保存在本机，用户自行配置第三方 AI 服务。

## 2. 产品边界

### 2.1 可信度原则

- 不把声音变化解释为用户的真实心理状态。
- 前台使用“表达稳定性”“表达明确度”等可观察训练语言。
- 没有有效文本、发言时长、声音片段或语义结果时，显示数据不足，不补默认分数。
- 合成语音可用于算法回归，不作为真人有效性的证明。
- 三个场景采用不同评价标准，场景总分不得直接横向比较。

### 2.2 隐私边界

- 默认后端只监听 `127.0.0.1`，当前不支持公网部署。
- 会话、配置、材料解析结果和个人声音基线保存在本机。
- 使用第三方 ASR、TTS 或 LLM 时，相应音频或文本会发送给用户配置的服务商。
- API Key 不回传明文，不进入日志或公开示例。
- 公共仓库不包含真实用户录音、简历、数据库或上传文件。

### 2.3 一期不做

- 新增第四个训练场景
- 登录、云端多用户、支付和排行榜
- 视频、肢体动作和微表情分析
- 心理、情绪或自信诊断
- Provider 数量扩张
- 桌面客户端和移动端原生应用

## 3. 核心架构

```text
共享事实层
录音 → 转写 → 个人基线 → 实时提示 → 表达与声音事实 → 本地保存
                                              ↓
场景评价层
面试评价 / 工作汇报评价 / 演讲评价
                                              ↓
场景化报告
```

### 3.1 共享事实层

共享核心只描述发生了什么，不判断该事实对某个场景意味着什么。

主要输出：

- 有效发言时长和语速
- 停顿数量与时长
- 口癖、模糊措辞和重复表达
- 超长句和文本重复率
- 可用声音片段中的音高、能量和稳定信号
- 是否使用个人基线
- 数据来源、样本状态和分析版本

### 3.2 场景评价层

每个 `ScenarioPack` 必须声明：

- 阶段及 AI 角色行为
- 所需材料与配置字段
- 评价维度、权重和版本
- LLM 语义评价标准
- 专项建议章节

新增或修改场景不得复制录音、转写、分析和持久化核心。

## 4. 三个训练场景

### 4.1 模拟面试

流程：开场 → 自我介绍 → 项目追问 → 岗位能力问题 → 反问 → 报告。

可选输入：简历、目标岗位、职级、公司和 JD。

重点评价：

- 回答结构：结论、论据与 STAR 结构
- 岗位匹配：回答证据与岗位、项目经历的相关性
- 表达流畅度、节奏、声音与个人基线稳定性

### 4.2 工作汇报

流程：开场 → 用户汇报 → 评审质询 → 收尾 → 报告。

可选输入：汇报主题、材料和目标时长。

重点评价：

- 结论与结构：是否结论先行、层次清晰
- 数据与论据：量化证据、行动证据和质询应对
- 时间控制、表达流畅度、节奏与个人基线稳定性

### 4.3 演讲训练

流程：主持开场 → 用户演讲 → 主持收尾 → 报告。AI 不在演讲中途提问。

可选输入：演讲主题、大纲或材料、目标时长。

重点评价：

- 演讲结构：开场、主体、转折和结尾
- 观点表达：核心观点是否鲜明、有记忆点
- 节奏、声音、时间控制与个人基线稳定性

## 5. 用户主流程

```text
打开本地网页
  ↓
首次使用可先查看固定示例报告
  ↓
完成 30 秒个人声音校准（可跳过，但稳定性不评分）
  ↓
选择训练场景
  ↓
填写当前场景需要的信息或上传材料
  ↓
开始训练
  ├─ AI 按场景阶段发言
  ├─ 用户语音实时转写
  └─ 系统提供低干扰表达提示
  ↓
结束训练
  ↓
查看：可观察事实 → 场景评价 → 下一次练习
```

## 6. 一期功能需求

### 6.1 P0：核心闭环

- 三个场景均可创建、开始、阶段推进、结束和生成报告。
- ASR 转写、TTS 播放和 LLM 评价可配置。
- 支持 PDF、DOCX、TXT 材料；不声明支持传统 DOC。
- 支持个人声音校准、重新校准和校准状态显示。
- 实时提示覆盖口癖、模糊措辞、重复、长句、语速和停顿。
- 句子级声音事实及真实发言时长持久化，报告复用同一份事实。
- 报告包含分析版本、评价版本、分数覆盖率、样本状态和证据。
- 没有有效数据时不生成默认分数。

### 6.2 P0：首次体验

- 首页一句话说明核心价值。
- 三个场景平等展示并说明训练目标。
- 固定示例报告无需 API Key，可直接从首页进入。
- 示例数据必须明确标注，不支持导出为真实报告。
- 360px 以上宽度不出现阻断性横向滚动。

### 6.3 P0：本地发布安全

- 后端默认监听回环地址。
- HTTP Host 和 WebSocket Origin 限制为受信本地来源。
- 配置读取只返回密钥是否存在，不返回明文。
- 更换 Provider 或 Base URL 时不得静默复用旧密钥。
- `.env`、数据库、上传文件、报告和真实录音不得进入 Git。

### 6.4 P1：工程质量

- 后端测试覆盖共享分析、三场景矩阵、配置安全和报告输出。
- 前端类型检查和生产构建可重复执行。
- GitHub Actions 在推送和 PR 上运行后端测试及前端构建。
- 前端 lint、依赖审计和后端静态检查在公开发布前接入。
- PDF 在 macOS/Linux 上使用可用中文字体，并安全转义用户和模型文本。

### 6.5 P1：验证与说明

- 每个场景至少准备一组正常表达与刻意问题表达的真人对比录音。
- 公开测试方法、结果、失败案例和局限。
- README 说明本地数据位置和第三方数据流向。
- 发布前确认许可证及所有示例资产的再分发权利。

## 7. 报告信息结构

### 7.1 可观察事实

- 有效发言时长、语速和总字数
- 口癖、重复率、长句和停顿
- 声音信号是否有效、是否使用个人基线

### 7.2 场景评价

- 当前场景声明的评价轴、权重和说明
- 确定性信号分与语义评价分的来源标识
- 语义评价的原话或事实证据
- 有效评价覆盖率和数据不足状态

### 7.3 下一次练习

- 少量短期改进动作
- 场景专项建议
- 可重复验证的中期练习方向

## 8. 一期验收

详细工作包和发布门槛见 [docs/phase-1-open-source-readiness.md](docs/phase-1-open-source-readiness.md)。

一期完成至少满足：

1. 三个场景各完成一次真实训练并生成对应报告。
2. 同一共享事实进入三个场景时数值一致，场景语义评价不同。
3. 无数据、未校准和外部服务失败时不会生成误导性结论。
4. 后端测试、前端构建和 GitHub Actions 全部通过。
5. 无需密钥的示例报告、README、隐私说明和已知局限齐全。
6. 许可证、评测音频和示例数据权利确认完成后再公开仓库。
